import 'package:flutter/material.dart';

/// A shared soft baby-blue accent, independent of the light/dark
/// palette (main.dart's _DarkPalette/_LightPalette) — a fixed-color
/// treatment, same idea as the semantic red Material3 already reserves
/// for wrong answers elsewhere in the app. Used wherever the Shop wants
/// a bit of color against its otherwise neutral palette: ShopScreen's
/// per-card language ribbon, BookDetailScreen's Free Sample button.
const babyBlueAccent = Color(0xFF89CFF0);
